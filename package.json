{
  "name": "forum_project",
  "version": "1.0.0",
  "description": "forum of projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"  cd server && npm run dev  \"       \"   cd client && npm start   \"  "
  },
  "author": "Slim-Developper",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
